{
  "description": "DanteForge session-start hook that injects workflow guidance and native slash-command context.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.mjs\" session-start"
          }
        ]
      }
    ]
  }
}
