---
name: ember
description: "Very low-token preset - budget autoforge with light checkpoints and loop detection"
---

# /ember - Light Preset

When the user invokes `/ember`, execute the light preset in the workspace:

1. Run budget-oriented autoforge with light checkpoints
2. Keep loop detection and hard gates enabled
3. Compact lessons as lightweight self-improvement cleanup

Use this for quick features, prototypes, and token-conscious follow-up work.

Options:
- `--prompt` - Show the preset plan without executing it
- `--profile quality|balanced|budget` - Override the default budget profile

CLI parity: `danteforge ember [goal]`
