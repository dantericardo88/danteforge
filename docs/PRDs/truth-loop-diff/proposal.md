# Proposal — truth-loop-diff

## Problem statement
ship "danteforge truth-loop diff <runIdA> <runIdB>" — a CLI subcommand that compares two truth-loop runs side-by-side.

## Goal
ship "danteforge truth-loop diff <runIdA> <runIdB>" — a CLI subcommand that compares two truth-loop runs side-by-side.

## Why now
Authored by /dante-to-prd from conversation context. Founder confirms timing in Phase 5 surfaced-assumptions review.

## Success metric (measurable)
CLI exits 0 with structured delta showing per-category claim count deltas, score delta, finalStatus pair

## Strategic context
Generated from conversation; refine via /dante-grill-me.
