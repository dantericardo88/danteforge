# Proposal — truth-loop-list

## Problem statement
ship "danteforge truth-loop list" — a CLI subcommand that enumerates prior truth-loop runs in .danteforge/truth-loop/ with a brief summary line per run.

## Goal
ship "danteforge truth-loop list" — a CLI subcommand that enumerates prior truth-loop runs in .danteforge/truth-loop/ with a brief summary line per run.

## Why now
Authored by /dante-to-prd from conversation context. Founder confirms timing in Phase 5 surfaced-assumptions review.

## Success metric (measurable)
CLI emits one line per run with runId + finalStatus + score + objective; --json flag outputs structured data; --limit N trims output

## Strategic context
Generated from conversation; refine via /dante-grill-me.
