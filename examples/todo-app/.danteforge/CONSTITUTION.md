# TODO CLI Example Constitution

- Keep the example local-first and runnable with plain Node.js.
- Use a single JSON file in the operator home directory for persistence.
- Keep business logic pure and isolate file I/O in `src/storage.js`.
- CLI errors must fail closed with clear usage messages.
- Ship only artifacts that truthfully match the example code and tests.
